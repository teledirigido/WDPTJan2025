/**
 * We call the package we installed on the command line and
 * assigned to our variable myColors
 * 
 */
const myColors = require('colors/safe');


/**
 * We use the method yellow to print a string
 */
console.log(myColors.yellow('Hello'));
console.log(myColors.rainbow('OMG Rainbows!'));