let x = 10; //Declare and initialize a variable
let y; //Declare a variable but not initilize
y = 20; //let can be reassinged

let a = 5,
  b = 10,
  c = 15;
//declaring multiple variables as once

console.log(a, b, c);

const pi = 3.14; //const can't be declared and not initialized
/* pi = 5; */ //Throws an error, as const can't be reassinged

const user = 'Eric',
  age = 28,
  isAdmin = true;
//Declaring multiple constants at once

console.log(pi, user, age, isAdmin);
